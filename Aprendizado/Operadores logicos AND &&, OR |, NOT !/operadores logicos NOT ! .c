#include <stdio.h>

int main() {

    //operação NOT segue o operador !, inverte o resultado final, se verdadeiro vira falso e vice versa 
// segue a tabela verdade NOT
    int a = 0;   //se vale 1 vira 0 se vale 0 vira 1

if (!a) {
  printf("a é zero.\n");
}
//sinalizar o else se quiser o retorno negativo






}