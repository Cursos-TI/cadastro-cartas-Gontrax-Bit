#include <stdio.h>

int main() {

    //operação OR segue o operador ||, so e negativo, falso = 0 se ambos forem negativos, falsos = 0
// segue a tabela verdade OR
    int a = -10, b = -15;

if (a > 0 || b > 0) {
  printf("Pelo menos um é positivo.\n");
}
//sinalizar o else se quiser o retorno negativo






}