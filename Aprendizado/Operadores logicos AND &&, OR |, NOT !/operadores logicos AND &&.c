#include <stdio.h>

int main() {

    //operação AND, so e possitivo, verdadeiro = 1 se ambos forem positivos, verdadeiros = 1
// segue a tabela verdade AND
    int a = 10, b = 15;

if (a > 0 && b > 0) {
  printf("Ambos são positivos.\n");
}
//sinalizar o else se quiser o retorno negativo






}